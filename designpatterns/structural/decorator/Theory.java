package designpatterns.structural.decorator;

public class Theory {
/**
 * Decorator design pattern
 *
 * 1.Dynamically adds or overrides functionality of an existing class at runtime.
 * 2.Promotes composition over inheritance to extend behavior.
 * 3.Class Explosion -Class explosion happens when you end up creating
 * too many classes in your codebase, often because you’re trying to cover
 * every possible combination of features, types, or behaviors.
 * This makes your code hard to manage, understand, and maintain.p
 *
 * we should always use composition over inheritance
 * */




/**
 * If I want to provide an object extra functionality at run time then at that time  I will use
 * decorator design pattern
 * Parent class hai - run(),
 * Child class hai - run()
 * Parent p = new Child()
 * p.run()
 *
 *
 * */


/**
 * Decorator pattern is used when you want to wrap
 * your object with multiple functionalities,
 * adding them dynamically and flexibly at runtime.
 * */
}
