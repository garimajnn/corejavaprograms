package designpatterns.creational.Builder;

public class Theory {

/**
 * Whenever your class is having multiple fields like there are 20 -50 -1000 fields
 * But you need to make use of certain fields only then you have to create so many constructor
 * dependeing on the field .
 * Then every time for different differnt scenario you need to create multiple constructor
 * It will make the code more tightly coupled  so in this case we  can create one builder design pattern
 * we can create one object  with the help of builder class and we can assign values to the particular
 * fields so we even do not need to create particular order which is the disadvantage in factory design
 * pattern.
 * Builder design pattern will give you that much flexibility to make use of those fields
 * which you need
 *
 *
 * */

}
